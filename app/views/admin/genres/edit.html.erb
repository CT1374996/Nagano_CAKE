<div class="container">
<h5>ジャンル編集</h5>
<div><%= form_with model: @genre, url: admin_genre_path(@genre.id), method: :patch do |f|%>
ジャンル名：<%= f.text_field :genre_name %>
<%= f.submit "変更を保存" %><% end %></div>
</div>