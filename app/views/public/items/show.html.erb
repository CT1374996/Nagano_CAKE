<h3><%= @item.name %></h3>
<div><%= @item.introduction %></div>
<div>￥<%= @item.price %>(税込み)</div>

<div>数量を選択して下さい。</div>
<%= form_with model: @item, url: cart_items_path, method: :post do |f| %>
<%= f.select :amount, *[1..10] %>個
<%= f.submit 'カートに入れる' %>
<% end %>